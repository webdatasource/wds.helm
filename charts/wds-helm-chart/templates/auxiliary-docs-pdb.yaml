{{- if .Values.global.auxiliaryServices.docs.enabled }}
  {{- $name := "docs" }}
  {{- $params := include "dictAddMissing" (list .Values.auxiliaryServices.docs.pdb .Values.global.pdb) | fromYaml -}}
  {{- if $params.enabled }}
    {{ include "wds-helm-chart.pdb" (dict
        "ctx" .
        "name" $name
        "minAvailable" $params.minAvailable
        "maxUnavailable" $params.maxUnavailable
      ) }}
  {{- end }}
{{- end }}