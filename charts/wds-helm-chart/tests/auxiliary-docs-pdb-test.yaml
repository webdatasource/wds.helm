suite: test docs PDB
templates:
  - auxiliary-docs-pdb.yaml
tests:
  - it: should not render when docs disabled
    set:
      global.auxiliaryServices.docs.enabled: false
      global.pdb.enabled: true
    asserts:
      - hasDocuments:
          count: 0

  - it: should render PDB when enabled globally
    set:
      global.auxiliaryServices.docs.enabled: true
      global.pdb.enabled: true
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: PodDisruptionBudget
      - equal:
          path: spec.minAvailable
          value: 1

  - it: should not render PDB when disable for the service
    set:
      global.auxiliaryServices.docs.enabled: true
      global.pdb.enabled: true
      auxiliaryServices.docs.pdb.enabled: false
    asserts:
      - hasDocuments:
          count: 0

  - it: should allow service-level PDB override values
    set:
      global.auxiliaryServices.docs.enabled: true
      global.pdb.enabled: true
      auxiliaryServices.docs.pdb.minAvailable: 2
      auxiliaryServices.docs.pdb.maxUnavailable: 1
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: PodDisruptionBudget
      - equal:
          path: spec.minAvailable
          value: 2
      - notExists:
          path: spec.maxUnavailable

