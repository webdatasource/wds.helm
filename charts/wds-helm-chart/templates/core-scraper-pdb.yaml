{{- if eq .Values.global.coreServices.mode "MultiService" }}
  {{- $name := "scraper" }}
  {{- $params := include "dictAddMissing" (list .Values.coreServices.scraper.pdb .Values.global.pdb) | fromYaml -}}
  {{- if $params.enabled }}
    {{ include "wds-helm-chart.pdb" (dict
        "ctx" .
        "name" $name
        "minAvailable" $params.minAvailable
        "maxUnavailable" $params.maxUnavailable
      ) }}
  {{- end }}
{{- end }}
