{{- if eq .Values.global.coreServices.mode "MultiService" }}
  {{- $name := "datakeeper" }}
  {{- $pdbEnabled := .Values.coreServices.datakeeper.pdb.enabled | default .Values.global.pdb.enabled }}
  {{- if $pdbEnabled }}
    {{ include "wds-helm-chart.pdb" (dict
        "ctx" .
        "name" $name
        "minAvailable" (.Values.coreServices.datakeeper.pdb.minAvailable | default .Values.global.pdb.minAvailable)
        "maxUnavailable" (.Values.coreServices.datakeeper.pdb.maxUnavailable | default .Values.global.pdb.maxUnavailable)
      ) }}
  {{- end }}
{{- end }}

